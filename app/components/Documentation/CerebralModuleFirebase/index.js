import React from 'react';

import Markdown from '../../Markdown';

export default function CerebralModuleFirebase() {
  return (
    <div>
      <Markdown filename="doc_cerebral-module-firebase.md" />
    </div>
  );
}
